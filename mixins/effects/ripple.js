export default {
    methods: {
        ripple_effect_function(event) {
            // let { width, height, x: element_x, y: element_y } = event.target.getBoundingClientRect();
            // const x_coordinate = event.clientX - parseInt(element_x);
            // const y_coordinate = event.clientY - parseInt(element_y);
            // const max_side = Math.max(width, height);
            // const ripple = document.createElement('span.ripple');
            // ripple.style.position = 'absolute';
            // ripple.style.left = x_coordinate + 'px';
            // ripple.style.top = y_coordinate + 'px';
            // ripple.style.transition = '1s !important';
            // ripple.style.background = 'rgba(122,122,122,.4)';
            // ripple.style.width = '0px';
            // ripple.style.height = '0px';
            // ripple.style.transform = 'translate(-50%, -50%)';
            // ripple.style.clipPath = 'circle(50%)';
            // // clip-path: circle(25%);
            // // transition: all 1s;
            // event.target.appendChild(ripple);
            // ripple.style.width = max_side + 'px';
            // ripple.style.height = max_side + 'px';
        }
    }
}